apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name | dump }}
  annotations:
    github.com/project-slug: Peyoz/aws-ec2 #name of the terraform repo
    backstage.io/techdocs-ref: dir:.
  title: 'Automation for instance deployment'
  description: An example of a Instance Creation.
spec:
  type: service
  owner: user:guest
  lifecycle: experimental